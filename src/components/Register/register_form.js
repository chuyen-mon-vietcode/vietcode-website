import React from "react"
import ReactDOM from "react-dom"

import {
    FormCategories,
    FormPlaceHolders,
    FormLabels,
    SheetUrl
} from "../../config/FormConfig"


const Form = () => {
    return <div></div>
}

export default Form;
